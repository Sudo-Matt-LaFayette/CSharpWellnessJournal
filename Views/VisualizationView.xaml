<UserControl x:Class="DailyCheckInJournal.Views.VisualizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Visualizations" FontSize="24" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,10"/>

        <!-- Date Range Selection -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="From:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding StartDate}" Margin="0,0,20,0"/>
            <TextBlock Text="To:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding EndDate}" Margin="0,0,20,0"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Padding="10,5"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Active Patterns -->
                <GroupBox Header="Active Patterns" Margin="0,0,0,20">
                    <ItemsControl ItemsSource="{Binding ActivePatterns}" Margin="10">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Orange" BorderThickness="2" Margin="0,5" Padding="10" Background="LightYellow">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock Text="{Binding Description}" Margin="0,5,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>

                <!-- Energy Level Chart -->
                <GroupBox Header="Energy Level Over Time" Margin="0,0,0,20">
                    <lvc:CartesianChart Series="{Binding EnergySeries}" Height="200" Margin="10"/>
                </GroupBox>

                <!-- Mood Chart -->
                <GroupBox Header="Mood Over Time" Margin="0,0,0,20">
                    <lvc:CartesianChart Series="{Binding MoodSeries}" Height="200" Margin="10"/>
                </GroupBox>

                <!-- Sleep Quality Chart -->
                <GroupBox Header="Sleep Quality Over Time" Margin="0,0,0,20">
                    <lvc:CartesianChart Series="{Binding SleepSeries}" Height="200" Margin="10"/>
                </GroupBox>

                <!-- Mistake Frequency Chart -->
                <GroupBox Header="Mistake Frequency">
                    <lvc:CartesianChart Series="{Binding MistakeFrequencySeries}" Height="200" Margin="10"/>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

