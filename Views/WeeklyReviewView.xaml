<UserControl x:Class="DailyCheckInJournal.Views.WeeklyReviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Weekly Review" FontSize="24" FontWeight="Bold" Grid.Row="0" Margin="0,0,0,10"/>

        <!-- Date Selection -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,20">
            <Button Content="&lt; Previous Week" Command="{Binding PreviousWeekCommand}" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding ReviewStartDate}" Margin="10,0"/>
            <TextBlock Text="to" VerticalAlignment="Center" Margin="10,0"/>
            <TextBlock Text="{Binding ReviewStartDate, StringFormat='{}{0:MMM dd, yyyy}'}" VerticalAlignment="Center" Margin="10,0"/>
            <Button Content="Next Week &gt;" Command="{Binding NextWeekCommand}" Margin="10,0,0,0"/>
            <Button Content="Generate Report" Command="{Binding GenerateReportCommand}" Margin="20,0,0,0" Padding="10,5"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Statistics -->
                <GroupBox Header="Weekly Statistics" Margin="0,0,0,15">
                    <Grid Margin="10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Average Energy" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding AverageEnergy, StringFormat='{}{0:F1}/10'}" FontSize="20" HorizontalAlignment="Center" Margin="0,5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Average Mood" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding AverageMood, StringFormat='{}{0:F1}/10'}" FontSize="20" HorizontalAlignment="Center" Margin="0,5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Must-Do Completion" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding MustDoCompletionRate, StringFormat='{}{0}%'}" FontSize="20" HorizontalAlignment="Center" Margin="0,5"/>
                        </StackPanel>
                        <StackPanel Grid.Column="3">
                            <TextBlock Text="Overcommitted Days" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding OvercommitmentCount}" FontSize="20" HorizontalAlignment="Center" Margin="0,5"/>
                        </StackPanel>
                    </Grid>
                </GroupBox>

                <!-- Detected Patterns -->
                <GroupBox Header="Detected Patterns" Margin="0,0,0,15">
                    <ItemsControl ItemsSource="{Binding DetectedPatterns}" Margin="10">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Orange" BorderThickness="2" Margin="0,5" Padding="10" Background="LightYellow">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14"/>
                                        <TextBlock Text="{Binding Description}" Margin="0,5,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>

                <!-- Weekly Summary Report -->
                <GroupBox Header="Weekly Summary Report" Margin="0,0,0,15">
                    <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto">
                        <TextBlock Text="{Binding WeeklySummary}" TextWrapping="Wrap" Margin="10" FontFamily="Consolas"/>
                    </ScrollViewer>
                </GroupBox>

                <!-- Daily Check-Ins -->
                <GroupBox Header="Daily Check-Ins">
                    <ItemsControl ItemsSource="{Binding WeeklyCheckIns}" Margin="10">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="Gray" BorderThickness="1" Margin="0,5" Padding="10">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Date, StringFormat='{}{0:dddd, MMMM dd, yyyy}'}" FontWeight="Bold"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                            <TextBlock Text="Morning: " FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Morning.EnergyLevel, StringFormat='Energy: {}{0}'}" Margin="10,0,20,0"/>
                                            <TextBlock Text="Evening: " FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Evening.EnergyLevel, StringFormat='Energy: {}{0}'}" Margin="10,0,20,0"/>
                                            <TextBlock Text="Must-Do: " FontWeight="Bold"/>
                                            <TextBlock Text="{Binding Evening.MustDoCompleted, StringFormat='{}{0}'}" Margin="10,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

